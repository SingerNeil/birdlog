# BirdLog v0.1 is a static app and does not need build-time env vars.
# Configure optional Supabase sync inside the app settings instead, so a public
# GitHub repository does not contain your personal sync configuration.
